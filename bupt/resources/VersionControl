20210823 1.实现从Pypi官网抓取指定包名的包的json信息
         2.从抓取的json信息中获取作者（包括维护者）信息、版本信息
         3.实现从给定的恶意软件包的setup.py文件中读取作者、版本等包的基本信息
         4.手动解压包 -> 自动解压包。

TODO
1. maloss是从PyPI官网给定的项目的json路径下，读取python包基本信息。现在要做的是从给定包的setup.py文件的setup字典中读取。√
2. 手动下载包 -> 自动下载包到指定的python包检测目录。×
3. 手动解压包 -> 自动解压包。√
4. 构建依赖关系图
5. 标定敏感的API
6. 静态分析检测、构建AST
7. 让python自动下载网站的所有文件